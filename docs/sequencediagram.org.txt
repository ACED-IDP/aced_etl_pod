title ACED ETL

User->DeployedCommons: login
DeployedCommons-->User: credentials.json
User->Helm:Secrets/credentials.json

User->Docker: build, deploy to quay.io
Docker-->Helm: image: dependencies

Helm->ETL: context: environment variables, mounted files


opt synthetic studies workflow
DevOpsUser->ETL: login
ETL->DeployedCommons: create programs, projects(requestor)
ETL->DeployedCommons: upload files(indexd,s3)
ETL->DeployedCommons: deploy metadata(graph,elastic)
end


opt submitted studies workflow
User->DeployedCommons: create programs, projects(requestor)
User->DeployedCommons: upload files(indexd,s3)
User->DeployedCommons: upload metadata(indexd,s3)
DevOpsUser->ETL: login
ETL->DeployedCommons: download metadata(indexd,s3)
ETL->DeployedCommons: deploy metadata(graph,elastic)
end
